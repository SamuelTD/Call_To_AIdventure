route:
  receiver: local-log
  group_by: [alertname]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: local-log
# Add a credential-backed receiver only in the deployment environment. Never
# commit webhook URLs or notification tokens.
